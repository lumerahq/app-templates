[project]
name = "fixed-asset-tracker"
version = "0.1.0"
description = "Fixed Asset Tracker - Lumera custom app"
requires-python = ">=3.11"
dependencies = [
    "lumera",
]

[project.optional-dependencies]
dev = [
    "ruff",
    "pytest",
]
