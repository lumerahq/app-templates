[project]
name = "payroll-journal-entry"
version = "0.1.0"
description = "Payroll Journal Entry - Lumera custom app"
requires-python = ">=3.11"
dependencies = [
    "lumera",
]

[project.optional-dependencies]
dev = [
    "ruff",
    "pytest",
]
