[project]
name = "invoice-processing"
version = "0.1.0"
description = "Invoice Processing - Lumera custom app"
requires-python = ">=3.11"
dependencies = [
    "lumera",
]

[project.optional-dependencies]
dev = [
    "ruff",
    "pytest",
]
