[project]
name = "{{projectName}}"
version = "0.1.0"
description = "{{projectTitle}} - Lumera custom app"
requires-python = ">=3.11"
dependencies = [
    "lumera",
]

[project.optional-dependencies]
dev = [
    "ruff",
    "pytest",
]
